# === Core ===
numpy>=1.23
gymnasium>=0.29
pybullet>=3.2.5
stable-baselines3>=2.3.0

# Torch: 若需GPU建议按官网命令单独安装带CUDA版本；此处留通用CPU/GPU均可的范围
torch>=2.1,<3.0

# === Logging & Visualization ===
tensorboard>=2.14

# === Optional utilities (按需启用) ===
# moviepy>=1.0.3         # 若 scripts/record_video.py 需要导出视频，可启用
# tqdm>=4.66.0           # 进度条（如需额外脚本使用）
# matplotlib>=3.7        # 如需画图保存训练曲线
